# 운명나침반 앱 환경 변수 설정 파일

# ===========================================
# 프론트엔드 환경 변수 (Vite)
# ===========================================

# API 서비스 URL (프로덕션)
VITE_API_URL=https://api-gateway-production.up.railway.app
VITE_CALENDAR_SERVICE_URL=https://calendar-service-production.up.railway.app
VITE_DIARY_SERVICE_URL=https://diary-service-production.up.railway.app
VITE_SAJU_SERVICE_URL=https://saju-service-production.up.railway.app

# API 서비스 URL (개발환경)
# VITE_API_URL=http://localhost:4000
# VITE_CALENDAR_SERVICE_URL=http://localhost:4012
# VITE_DIARY_SERVICE_URL=http://localhost:4004
# VITE_SAJU_SERVICE_URL=http://localhost:4015

# ===========================================
# 백엔드 환경 변수 (Railway)
# ===========================================

# 서버 포트 설정
PORT=8080
NODE_ENV=production

# 데이터베이스 연결 (PostgreSQL)
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# CORS 설정
CORS_ORIGIN=https://your-vercel-app.vercel.app

# JWT 인증 (향후 구현)
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# ===========================================
# 로컬 개발 환경 변수
# ===========================================

# Calendar Service
CALENDAR_SERVICE_PORT=4012

# Diary Service
DIARY_SERVICE_PORT=4004

# Saju Analysis Service
SAJU_SERVICE_PORT=4015

# SQLite 데이터베이스 경로 (개발용)
SQLITE_DB_PATH=./saju.db

# ===========================================
# 외부 서비스 API 키 (선택사항)
# ===========================================

# 음력 달력 API (향후 구현)
LUNAR_CALENDAR_API_KEY=your-lunar-calendar-api-key

# 사주 분석 API (향후 구현)
SAJU_ANALYSIS_API_KEY=your-saju-api-key

# 이메일 서비스 (향후 구현)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password

# ===========================================
# 보안 설정
# ===========================================

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 암호화 키
ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# 모니터링 및 로깅
# ===========================================

# 로그 레벨 (development, production)
LOG_LEVEL=info

# Sentry DSN (에러 추적)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ===========================================
# 성능 설정
# ===========================================

# 캐시 TTL (초)
CACHE_TTL=3600

# 최대 파일 업로드 크기 (MB)
MAX_FILE_SIZE=10